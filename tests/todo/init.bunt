class Circle {
    init(radius) {
        this.radius = radius;
    }

// don't support properties yet.
    area {
        return 3.14 * this.radius * this.radius;
    }
}

var circle = Circle(4);
print circle.area;